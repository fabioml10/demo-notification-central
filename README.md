## Pre-requisites:
1. Install Docker
2. Install Docker Compose
3. Clone the repository
4. Create empty `.env` file in notification_center project

## Running:
```bash
docker compose build
```

```bash
docker compose up
```

Have Fun!
